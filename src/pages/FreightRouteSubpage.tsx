import type { FC } from 'react';

/**
 * Sub-route component placeholder.
 * All active corridor content is now handled directly on the main France–Angola route page.
 */
const FreightRouteSubpage: FC = () => {
  return null;
};

export default FreightRouteSubpage;