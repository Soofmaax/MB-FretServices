# Base URL of the site for canonical URLs and structured data
VITE_SITE_URL=https://mb-fretservices.com

# Base path for assets and routing (used by Vite). For GitHub Project Pages, set to "/REPO-NAME/".
# For user/organization pages or Cloudflare/Netlify, leave as "/".
# Example: VITE_BASE=/mb-fretservices/
# VITE_BASE=/

# Default Open Graph image (absolute URL or path under /public, e.g. /og-default.webp)
# Now using a local, optimized asset generated by scripts/fetch-assets.mjs
VITE_OG_IMAGE=/og-default.webp

# Supported languages for SEO alternates (affects hreflang/og:locale:alternate on client)
# Comma-separated, e.g. "fr,en,pt". Leave empty to use all defaults.
# This does not affect sitemap/ssg; see SITEMAP_LANGS / PRERENDER_LANGS for scripts.
VITE_SUP_LANGS=

# Google Analytics 4 Measurement ID (e.g. G-XXXXXXXXXX)
# Leave empty to disable tracking in this environment
VITE_GA_ID=

# Microsoft Clarity Project ID (optional)
# Leave empty to disable Clarity
VITE_CLARITY_ID=

# Indexing control: languages allowed to be indexed (client-side)
# Comma-separated, e.g. "fr,en,pt,es,tr,de,it,sw,ar"
VITE_LANG_INDEX=

# Scripts control (build-time)
# Limit languages for sitemap, prerender and ai.txt (comma-separated)
SITEMAP_LANGS=
PRERENDER_LANGS=
AI_TXT_LANGS=

# Search engine verification tokens (optional)
# Google Search Console
VITE_GSC_VERIFICATION=
# Bing Webmaster Tools
VITE_BING_VERIFICATION=